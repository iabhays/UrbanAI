# Edge Deployment Configuration

This directory contains configuration files for edge deployment scenarios.

## Files

- `edge_config.yaml`: Edge-specific configuration
- `tensorrt_config.yaml`: TensorRT optimization settings
- `gpu_config.yaml`: GPU-specific settings

## Usage

Copy relevant configuration files to your edge deployment and update paths as needed.
