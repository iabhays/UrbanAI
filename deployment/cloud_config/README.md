# Cloud Deployment Configuration

This directory contains configuration files for cloud deployment scenarios.

## Files

- `kubernetes/`: Kubernetes manifests
- `aws/`: AWS-specific configurations
- `gcp/`: Google Cloud Platform configurations
- `azure/`: Azure-specific configurations

## Usage

Select the appropriate cloud provider configuration and deploy using the provided manifests.
